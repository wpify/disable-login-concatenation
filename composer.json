{
	"name": "wpify/disable-login-concatenation",
	"description": "Disables script/style concatenation on the WordPress login page so styles load correctly when load-styles.php and load-scripts.php are blocked by the web server.",
	"type": "wordpress-muplugin",
	"license": "GPL-2.0-or-later",
	"authors": [
		{
			"name": "WPify",
			"homepage": "https://wpify.io"
		}
	],
	"require": {
		"php": ">=7.4",
		"composer/installers": "^1.0 || ^2.0"
	},
	"keywords": [
		"wordpress",
		"mu-plugin",
		"security",
		"login",
		"load-styles",
		"load-scripts"
	]
}
